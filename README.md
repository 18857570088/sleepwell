# sleepwell

Native Android APP for the Sleepwell interface prototypes.

The project uses:

- Android SDK: `D:\2026\codexwork\AndroidSDK`
- Gradle user home: `D:\2026\codexwork\.gradle`
- Source directory: `D:\2026\202605\sleepwell`

Build:

```bat
gradlew.bat assembleDebug
```

The UI is implemented with native Kotlin views. The supplied HTML files remain in
`design/` as reference material only and are not loaded by the app.
